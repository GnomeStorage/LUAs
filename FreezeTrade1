-- Fake Freeze Trade UI by Bacon Scripts
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local FreezeTrade = Instance.new("TextButton")
local ForceAccept = Instance.new("TextButton")
local Footer = Instance.new("TextLabel")
local dragging, dragInput, dragStart, startPos

-- For compatibility with Delta/KRNL
if syn then
    ScreenGui.Parent = game:GetService("CoreGui")
else
    ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
end
ScreenGui.Name = "FakeFreezeTradeUI"

-- UI Main Frame
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(222, 243, 222)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.3, 0, 0.3, 0)
MainFrame.Size = UDim2.new(0, 250, 0, 160)
MainFrame.Active = true
MainFrame.Draggable = false

-- Title
Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "❄️ FREEZE TRADE"
Title.TextColor3 = Color3.fromRGB(50, 90, 50)
Title.TextSize = 20

-- Freeze Trade Button
FreezeTrade.Name = "FreezeTrade"
FreezeTrade.Parent = MainFrame
FreezeTrade.BackgroundColor3 = Color3.fromRGB(180, 235, 255) -- snow-like blue
FreezeTrade.BorderSizePixel = 0
FreezeTrade.Position = UDim2.new(0.1, 0, 0.3, 0)
FreezeTrade.Size = UDim2.new(0.8, 0, 0, 35)
FreezeTrade.Font = Enum.Font.GothamBold
FreezeTrade.Text = "❄️ FREEZE TRADE"
FreezeTrade.TextColor3 = Color3.fromRGB(30, 30, 30)
FreezeTrade.TextSize = 16

-- Force Accept Button
ForceAccept.Name = "ForceAccept"
ForceAccept.Parent = MainFrame
ForceAccept.BackgroundColor3 = Color3.fromRGB(200, 255, 200)
ForceAccept.BorderSizePixel = 0
ForceAccept.Position = UDim2.new(0.1, 0, 0.6, 0)
ForceAccept.Size = UDim2.new(0.8, 0, 0, 35)
ForceAccept.Font = Enum.Font.GothamBold
ForceAccept.Text = "🔄 FORCE ACCEPT"
ForceAccept.TextColor3 = Color3.fromRGB(30, 30, 30)
ForceAccept.TextSize = 16

-- Footer Text
Footer.Name = "Footer"
Footer.Parent = MainFrame
Footer.BackgroundTransparency = 1
Footer.Position = UDim2.new(0, 0, 0.88, 0)
Footer.Size = UDim2.new(1, 0, 0.1, 0)
Footer.Font = Enum.Font.Gotham
Footer.Text = "Made by Bacon Scripts"
Footer.TextColor3 = Color3.fromRGB(80, 100, 80)
Footer.TextSize = 14

-- Fake loading effect on FreezeTrade
local isLoading = false
FreezeTrade.MouseButton1Click:Connect(function()
    if isLoading then return end
    isLoading = true
    local originalText = FreezeTrade.Text
    FreezeTrade.Text = "❄️ FREEZING."
    for i = 1, 3 do
        wait(0.4)
        FreezeTrade.Text = "❄️ FREEZING" .. string.rep(".", i)
    end
    wait(0.5)
    FreezeTrade.Text = "❄️ FROZEN ✅"
    wait(1)
    FreezeTrade.Text = originalText
    isLoading = false
end)

-- Fake click on ForceAccept
ForceAccept.MouseButton1Click:Connect(function()
    ForceAccept.Text = "✅ TRADE ACCEPTED"
    wait(1)
    ForceAccept.Text = "🔄 FORCE ACCEPT"
end)

-- Dragging for PC + Mobile
local function dragify(frame)
	local UIS = game:GetService("UserInputService")
	local dragging = false
	local dragInput, dragStart, startPos

	frame.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = frame.Position

			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)

	frame.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)

	game:GetService("UserInputService").InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end

dragify(MainFrame)
